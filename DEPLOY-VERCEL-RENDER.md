# Инструкция по деплою проекта на Vercel + Render

## 📋 Обзор

- **Frontend (Portal)**: Vercel
- **Backend (API)**: Render
- **Database (PostgreSQL)**: Render (free tier)

---

## 🚀 Шаг 1: Подготовка репозитория

1. Убедитесь, что ваш проект находится в Git репозитории (GitHub, GitLab или Bitbucket)
2. Все изменения должны быть закоммичены и запушены

---

## 🗄️ Шаг 2: Деплой PostgreSQL на Render

### 2.1 Создание базы данных

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь/войдите
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `ecof-portal-db`
   - **Database**: `ecof_portal`
   - **User**: `ecof_user`
   - **Plan**: **Free** (для начала)
   - **Region**: выберите ближайший (например, Frankfurt)
4. Нажмите **"Create Database"**
5. **ВАЖНО**: Сохраните строку подключения (Connection String) - она понадобится позже

### 2.2 Получение данных подключения

После создания БД, Render покажет:
- **Host**: `dpg-xxxxx-a.frankfurt-postgres.render.com`
- **Port**: `5432`
- **Database**: `ecof_portal`
- **User**: `ecof_user`
- **Password**: (будет показан один раз, сохраните!)

---

## ⚙️ Шаг 3: Деплой Backend на Render

### 3.1 Создание Web Service

1. В Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш Git репозиторий
3. Выберите репозиторий с проектом
4. Настройте сервис:
   - **Name**: `ecof-portal-backend`
   - **Environment**: **Node**
   - **Region**: тот же, что и для БД
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: `backend`
   - **Build Command**: `npm install && npm run build`
     - *Примечание*: Если сборка падает из-за ошибки `msnodesqlv8`, измените на: `npm install --no-optional && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: **Free**

### 3.2 Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

#### Обязательные переменные:

```bash
NODE_ENV=production
PORT=10000

# CORS - будет заменено после деплоя фронтенда
CORS_ORIGIN=https://your-project.vercel.app

# JWT Secret - ОБЯЗАТЕЛЬНО сгенерируйте случайную строку!
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this

JWT_EXPIRES_IN=7d

# Database (из вашей PostgreSQL БД на Render)
DB_HOST=dpg-xxxxx-a.frankfurt-postgres.render.com
DB_PORT=5432
DB_NAME=ecof_portal
DB_USER=ecof_user
DB_PASSWORD=your-db-password-from-render

# 1С УХ Integration (настройте под вашу 1С)
UH_API_URL=https://your-1c-server:8035/your-database/hs/ecof
UH_API_USER=Администратор
UH_API_PASSWORD=your-1c-password
UH_1C_USER=Администратор
UH_1C_PASSWORD=your-1c-password
UH_API_TIMEOUT=30000
UH_API_DEBUG=false
UH_API_INSECURE=true
UH_RETRY_ATTEMPTS=3
UH_RETRY_DELAY=5000
UH_QUEUE_INTERVAL=5000
UH_SYNC_INTERVAL=60000
ANALYTICS_WEBHOOK_INTERVAL=5000
OBJECTS_WEBHOOK_INTERVAL=5000
```

**Примечание**: 
- `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` можно взять из строки подключения Render PostgreSQL
- `CORS_ORIGIN` пока оставьте как есть, обновим после деплоя фронтенда
- **ВАЖНО**: На Render (Linux) прямое подключение к БД 1С через `msnodesqlv8` не работает. Используйте только HTTP API (`UH_API_URL`). Переменные `UH_DB_TYPE`, `UH_MSSQL_*` не нужны для Render.

### 3.3 Запуск деплоя

1. Нажмите **"Create Web Service"**
2. Render начнёт сборку и деплой
3. Дождитесь завершения (обычно 3-5 минут)
4. После успешного деплоя вы получите URL вида: `https://ecof-portal-backend.onrender.com`
5. Проверьте, что сервис работает: откройте `https://your-backend-url.onrender.com/health` - должно вернуть `{"status":"ok"}`

---

## 🎨 Шаг 4: Деплой Frontend на Vercel

### 4.1 Подключение репозитория

1. Зайдите на [vercel.com](https://vercel.com) и зарегистрируйтесь/войдите (можно через GitHub)
2. Нажмите **"Add New..."** → **"Project"**
3. Импортируйте ваш Git репозиторий
4. Выберите репозиторий с проектом

### 4.2 Настройка проекта

1. **Framework Preset**: Vite (должен определиться автоматически)
2. **Root Directory**: `portal` (если проект в подпапке)
3. **Build Command**: `npm run build` (по умолчанию)
4. **Output Directory**: `dist` (по умолчанию для Vite)
5. **Install Command**: `npm install` (по умолчанию)

### 4.3 Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

```bash
VITE_API_URL=https://your-backend-url.onrender.com/api
```

**ВАЖНО**: Замените `your-backend-url.onrender.com` на реальный URL вашего backend из Render!

### 4.4 Запуск деплоя

1. Нажмите **"Deploy"**
2. Vercel начнёт сборку и деплой
3. После завершения вы получите URL вида: `https://your-project.vercel.app`
4. Откройте URL в браузере - должен загрузиться портал

---

## 🔗 Шаг 5: Связывание Frontend и Backend

### 5.1 Обновление CORS в Backend

1. Вернитесь в Render → ваш Backend сервис
2. Откройте **"Environment"** → **"Environment Variables"**
3. Найдите `CORS_ORIGIN` и замените на URL вашего Vercel проекта:
   ```
   CORS_ORIGIN=https://your-project.vercel.app
   ```
4. Нажмите **"Save Changes"** - Render автоматически перезапустит сервис

### 5.2 Проверка работы

1. Откройте ваш Vercel URL в браузере
2. Попробуйте залогиниться или выполнить любое действие, которое обращается к API
3. Откройте консоль браузера (F12) - не должно быть ошибок CORS

---

## 🗃️ Шаг 6: Применение миграций БД

Backend автоматически применяет миграции при старте (см. `backend/src/index.ts`), но если нужно применить вручную:

### Вариант 1: Через Render Shell

1. В Render откройте ваш Backend сервис
2. Перейдите в **"Shell"** (в боковом меню)
3. Выполните:
   ```bash
   npm run migrate
   ```

### Вариант 2: Локально (если настроен доступ к БД)

Если у вас есть доступ к Render PostgreSQL извне (через VPN или whitelist IP):

```bash
cd backend
# Создайте .env с переменными из Render
npm run migrate
```

---

## ⚠️ Важные замечания

### Render Free Tier ограничения:

1. **Сервисы засыпают** после 15 минут простоя
   - Первый запрос после простоя может занять 30-60 секунд (cold start)
   - Для продакшена рассмотрите платный план ($7/мес)

2. **PostgreSQL Free Tier**:
   - База данных может быть удалена через 90 дней без активности
   - Для постоянного использования нужен платный план ($7/мес)

3. **Лимиты ресурсов**:
   - Free tier: 512MB RAM, 0.5 CPU
   - Для больших нагрузок может не хватить

### Безопасность:

1. **JWT_SECRET**: ОБЯЗАТЕЛЬНО измените на случайную строку минимум 32 символа
2. **DB_PASSWORD**: Храните в секретах, не коммитьте в Git
3. **UH_API_PASSWORD**: Не коммитьте пароли от 1С

### Мониторинг:

1. **Render Dashboard**: Следите за логами и метриками
2. **Vercel Analytics**: Включите в настройках проекта для мониторинга фронтенда

---

## 🔧 Troubleshooting

### Backend не запускается:

1. Проверьте логи в Render Dashboard
2. Убедитесь, что все переменные окружения заданы
3. Проверьте, что `npm run build` выполняется успешно

### Ошибка сборки `msnodesqlv8`:

Если видите ошибку типа `fatal error: sql.h: No such file or directory` или `libodbc.so.2: cannot open shared object file`:

**Решение**: Это нормально! Модуль `msnodesqlv8` помечен как опциональный (`optionalDependencies`) и не нужен на Render (Linux). Он используется только для прямого подключения к БД 1С на Windows.

Если сборка падает из-за этого:
1. В Render Dashboard → ваш Backend сервис → Settings → Build & Deploy
2. Измените Build Command на: `npm install --no-optional && npm run build`
3. Сохраните и перезапустите деплой

**Важно**: На Render используйте только HTTP API (`UH_API_URL`), а не прямое подключение к БД 1С.

### CORS ошибки:

1. Убедитесь, что `CORS_ORIGIN` в backend указывает на правильный Vercel URL
2. Проверьте, что URL без слеша в конце: `https://your-project.vercel.app` (не `https://your-project.vercel.app/`)

### База данных недоступна:

1. Проверьте, что PostgreSQL сервис запущен в Render
2. Убедитесь, что переменные `DB_*` правильные
3. Проверьте, что backend и БД в одном регионе

### Frontend не подключается к API:

1. Проверьте переменную `VITE_API_URL` в Vercel
2. Убедитесь, что URL заканчивается на `/api`
3. Проверьте консоль браузера на ошибки

---

## 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Vercel Documentation](https://vercel.com/docs)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)

---

## ✅ Чеклист деплоя

- [ ] PostgreSQL создан на Render
- [ ] Backend задеплоен на Render
- [ ] Все переменные окружения настроены в Backend
- [ ] Backend отвечает на `/health`
- [ ] Frontend задеплоен на Vercel
- [ ] `VITE_API_URL` настроен в Vercel
- [ ] `CORS_ORIGIN` обновлён в Backend на Vercel URL
- [ ] Миграции применены
- [ ] Порталом можно пользоваться из браузера

---

**Готово!** Ваш проект должен быть доступен по адресу Vercel. 🎉
